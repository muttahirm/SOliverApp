import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    borderWidth: 1,
    borderRadius: 8,
    borderColor: 'black',
    paddingVertical: 10,
    paddingHorizontal: 20,
    alignSelf: 'center',
  },
  buttonTitle: {
    fontSize: 14,
  },
});
